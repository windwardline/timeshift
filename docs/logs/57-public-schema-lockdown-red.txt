
> timeshift@0.1.0 test:run
> vitest run security-rls.test.ts


 RUN  v4.1.10 /home/user/timeshift

 ❯ security-rls.test.ts (4 tests | 3 failed) 37ms
     × enables row-level security on every modelled table 18ms
     × revokes the PostgREST grants anon and authenticated ride on 10ms
     × revokes default privileges so a future table is not auto-granted 7ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 3 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  security-rls.test.ts > public-schema lockdown (prisma/migrations) > enables row-level security on every modelled table
AssertionError: User has no "ALTER TABLE \"User\" ENABLE ROW LEVEL SECURITY" — a table without it is readable by anyone holding the Supabase publishable key: expected '-- CreateTable\nCREATE TABLE "User" (…' to contain 'ALTER TABLE "User" ENABLE ROW LEVEL S…'

- Expected
+ Received

- ALTER TABLE "User" ENABLE ROW LEVEL SECURITY;
+ -- CreateTable
+ CREATE TABLE "User" (
+     "id" TEXT NOT NULL,
+     "email" TEXT NOT NULL,
+     "passwordHash" TEXT NOT NULL,
+     "homeTimeZone" TEXT NOT NULL,
+     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "User_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateTable
+ CREATE TABLE "Trip" (
+     "id" TEXT NOT NULL,
+     "name" TEXT NOT NULL,
+     "destination" TEXT NOT NULL,
+     "userId" TEXT NOT NULL,
+     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "Trip_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateTable
+ CREATE TABLE "FlightSegment" (
+     "id" TEXT NOT NULL,
+     "tripId" TEXT NOT NULL,
+     "sequence" INTEGER NOT NULL,
+     "departureAirport" TEXT NOT NULL,
+     "arrivalAirport" TEXT NOT NULL,
+     "departureTime" TIMESTAMP(3) NOT NULL,
+     "arrivalTime" TIMESTAMP(3) NOT NULL,
+     "departureTz" TEXT NOT NULL,
+     "arrivalTz" TEXT NOT NULL,
+     "departureLat" DOUBLE PRECISION,
+     "departureLng" DOUBLE PRECISION,
+     "arrivalLat" DOUBLE PRECISION,
+     "arrivalLng" DOUBLE PRECISION,
+
+     CONSTRAINT "FlightSegment_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "User_email_key" ON "User"("email");
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "FlightSegment_tripId_sequence_key" ON "FlightSegment"("tripId", "sequence");
+
+ -- AddForeignKey
+ ALTER TABLE "Trip" ADD CONSTRAINT "Trip_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;
+
+ -- AddForeignKey
+ ALTER TABLE "FlightSegment" ADD CONSTRAINT "FlightSegment_tripId_fkey" FOREIGN KEY ("tripId") REFERENCES "Trip"("id") ON DELETE CASCADE ON UPDATE CASCADE;
+
+ -- CreateTable
+ CREATE TABLE "Session" (
+     "id" TEXT NOT NULL,
+     "token" TEXT NOT NULL,
+     "userId" TEXT NOT NULL,
+     "expiresAt" TIMESTAMP(3) NOT NULL,
+     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "Session_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "Session_token_key" ON "Session"("token");
+
+ -- AddForeignKey
+ ALTER TABLE "Session" ADD CONSTRAINT "Session_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;
+
+ -- AlterTable
+ ALTER TABLE "User" ALTER COLUMN "passwordHash" DROP NOT NULL;
+
+ -- CreateTable
+ CREATE TABLE "LoginToken" (
+     "id" TEXT NOT NULL,
+     "token" TEXT NOT NULL,
+     "email" TEXT NOT NULL,
+     "expiresAt" TIMESTAMP(3) NOT NULL,
+     "usedAt" TIMESTAMP(3),
+     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "LoginToken_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "LoginToken_token_key" ON "LoginToken"("token");
+
+ -- AlterTable
+ ALTER TABLE "FlightSegment" ADD COLUMN     "flightNumber" TEXT;
+
+ -- CreateTable
+ CREATE TABLE "FlightQueryCache" (
+     "id" TEXT NOT NULL,
+     "queryKey" TEXT NOT NULL,
+     "payload" JSONB NOT NULL,
+     "fetchedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "FlightQueryCache_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "FlightQueryCache_queryKey_key" ON "FlightQueryCache"("queryKey");
+

 ❯ security-rls.test.ts:40:9
     38|         `${model} has no "ALTER TABLE \\"${model}\\" ENABLE ROW LEVEL …
     39|           'a table without it is readable by anyone holding the Supaba…
     40|       ).toContain(`ALTER TABLE "${model}" ENABLE ROW LEVEL SECURITY;`);
       |         ^
     41|     }
     42|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/3]⎯

 FAIL  security-rls.test.ts > public-schema lockdown (prisma/migrations) > revokes the PostgREST grants anon and authenticated ride on
AssertionError: expected '-- CreateTable\nCREATE TABLE "User" (…' to contain 'ARRAY[\'anon\', \'authenticated\']'

- Expected
+ Received

- ARRAY['anon', 'authenticated']
+ -- CreateTable
+ CREATE TABLE "User" (
+     "id" TEXT NOT NULL,
+     "email" TEXT NOT NULL,
+     "passwordHash" TEXT NOT NULL,
+     "homeTimeZone" TEXT NOT NULL,
+     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "User_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateTable
+ CREATE TABLE "Trip" (
+     "id" TEXT NOT NULL,
+     "name" TEXT NOT NULL,
+     "destination" TEXT NOT NULL,
+     "userId" TEXT NOT NULL,
+     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "Trip_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateTable
+ CREATE TABLE "FlightSegment" (
+     "id" TEXT NOT NULL,
+     "tripId" TEXT NOT NULL,
+     "sequence" INTEGER NOT NULL,
+     "departureAirport" TEXT NOT NULL,
+     "arrivalAirport" TEXT NOT NULL,
+     "departureTime" TIMESTAMP(3) NOT NULL,
+     "arrivalTime" TIMESTAMP(3) NOT NULL,
+     "departureTz" TEXT NOT NULL,
+     "arrivalTz" TEXT NOT NULL,
+     "departureLat" DOUBLE PRECISION,
+     "departureLng" DOUBLE PRECISION,
+     "arrivalLat" DOUBLE PRECISION,
+     "arrivalLng" DOUBLE PRECISION,
+
+     CONSTRAINT "FlightSegment_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "User_email_key" ON "User"("email");
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "FlightSegment_tripId_sequence_key" ON "FlightSegment"("tripId", "sequence");
+
+ -- AddForeignKey
+ ALTER TABLE "Trip" ADD CONSTRAINT "Trip_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;
+
+ -- AddForeignKey
+ ALTER TABLE "FlightSegment" ADD CONSTRAINT "FlightSegment_tripId_fkey" FOREIGN KEY ("tripId") REFERENCES "Trip"("id") ON DELETE CASCADE ON UPDATE CASCADE;
+
+ -- CreateTable
+ CREATE TABLE "Session" (
+     "id" TEXT NOT NULL,
+     "token" TEXT NOT NULL,
+     "userId" TEXT NOT NULL,
+     "expiresAt" TIMESTAMP(3) NOT NULL,
+     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "Session_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "Session_token_key" ON "Session"("token");
+
+ -- AddForeignKey
+ ALTER TABLE "Session" ADD CONSTRAINT "Session_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;
+
+ -- AlterTable
+ ALTER TABLE "User" ALTER COLUMN "passwordHash" DROP NOT NULL;
+
+ -- CreateTable
+ CREATE TABLE "LoginToken" (
+     "id" TEXT NOT NULL,
+     "token" TEXT NOT NULL,
+     "email" TEXT NOT NULL,
+     "expiresAt" TIMESTAMP(3) NOT NULL,
+     "usedAt" TIMESTAMP(3),
+     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "LoginToken_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "LoginToken_token_key" ON "LoginToken"("token");
+
+ -- AlterTable
+ ALTER TABLE "FlightSegment" ADD COLUMN     "flightNumber" TEXT;
+
+ -- CreateTable
+ CREATE TABLE "FlightQueryCache" (
+     "id" TEXT NOT NULL,
+     "queryKey" TEXT NOT NULL,
+     "payload" JSONB NOT NULL,
+     "fetchedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "FlightQueryCache_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "FlightQueryCache_queryKey_key" ON "FlightQueryCache"("queryKey");
+

 ❯ security-rls.test.ts:46:17
     44|   it('revokes the PostgREST grants anon and authenticated ride on', ()…
     45|     const sql = migrationSql();
     46|     expect(sql).toContain("ARRAY['anon', 'authenticated']");
       |                 ^
     47|     expect(sql).toContain('REVOKE ALL ON ALL TABLES IN SCHEMA public F…
     48|   });

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/3]⎯

 FAIL  security-rls.test.ts > public-schema lockdown (prisma/migrations) > revokes default privileges so a future table is not auto-granted
AssertionError: expected '-- CreateTable\nCREATE TABLE "User" (…' to contain 'ALTER DEFAULT PRIVILEGES IN SCHEMA pu…'

- Expected
+ Received

- ALTER DEFAULT PRIVILEGES IN SCHEMA public REVOKE ALL ON TABLES FROM %I
+ -- CreateTable
+ CREATE TABLE "User" (
+     "id" TEXT NOT NULL,
+     "email" TEXT NOT NULL,
+     "passwordHash" TEXT NOT NULL,
+     "homeTimeZone" TEXT NOT NULL,
+     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "User_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateTable
+ CREATE TABLE "Trip" (
+     "id" TEXT NOT NULL,
+     "name" TEXT NOT NULL,
+     "destination" TEXT NOT NULL,
+     "userId" TEXT NOT NULL,
+     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "Trip_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateTable
+ CREATE TABLE "FlightSegment" (
+     "id" TEXT NOT NULL,
+     "tripId" TEXT NOT NULL,
+     "sequence" INTEGER NOT NULL,
+     "departureAirport" TEXT NOT NULL,
+     "arrivalAirport" TEXT NOT NULL,
+     "departureTime" TIMESTAMP(3) NOT NULL,
+     "arrivalTime" TIMESTAMP(3) NOT NULL,
+     "departureTz" TEXT NOT NULL,
+     "arrivalTz" TEXT NOT NULL,
+     "departureLat" DOUBLE PRECISION,
+     "departureLng" DOUBLE PRECISION,
+     "arrivalLat" DOUBLE PRECISION,
+     "arrivalLng" DOUBLE PRECISION,
+
+     CONSTRAINT "FlightSegment_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "User_email_key" ON "User"("email");
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "FlightSegment_tripId_sequence_key" ON "FlightSegment"("tripId", "sequence");
+
+ -- AddForeignKey
+ ALTER TABLE "Trip" ADD CONSTRAINT "Trip_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;
+
+ -- AddForeignKey
+ ALTER TABLE "FlightSegment" ADD CONSTRAINT "FlightSegment_tripId_fkey" FOREIGN KEY ("tripId") REFERENCES "Trip"("id") ON DELETE CASCADE ON UPDATE CASCADE;
+
+ -- CreateTable
+ CREATE TABLE "Session" (
+     "id" TEXT NOT NULL,
+     "token" TEXT NOT NULL,
+     "userId" TEXT NOT NULL,
+     "expiresAt" TIMESTAMP(3) NOT NULL,
+     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "Session_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "Session_token_key" ON "Session"("token");
+
+ -- AddForeignKey
+ ALTER TABLE "Session" ADD CONSTRAINT "Session_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;
+
+ -- AlterTable
+ ALTER TABLE "User" ALTER COLUMN "passwordHash" DROP NOT NULL;
+
+ -- CreateTable
+ CREATE TABLE "LoginToken" (
+     "id" TEXT NOT NULL,
+     "token" TEXT NOT NULL,
+     "email" TEXT NOT NULL,
+     "expiresAt" TIMESTAMP(3) NOT NULL,
+     "usedAt" TIMESTAMP(3),
+     "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "LoginToken_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "LoginToken_token_key" ON "LoginToken"("token");
+
+ -- AlterTable
+ ALTER TABLE "FlightSegment" ADD COLUMN     "flightNumber" TEXT;
+
+ -- CreateTable
+ CREATE TABLE "FlightQueryCache" (
+     "id" TEXT NOT NULL,
+     "queryKey" TEXT NOT NULL,
+     "payload" JSONB NOT NULL,
+     "fetchedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
+
+     CONSTRAINT "FlightQueryCache_pkey" PRIMARY KEY ("id")
+ );
+
+ -- CreateIndex
+ CREATE UNIQUE INDEX "FlightQueryCache_queryKey_key" ON "FlightQueryCache"("queryKey");
+

 ❯ security-rls.test.ts:53:28
     51|     // Without this, the next `prisma migrate` silently hands `anon` a…
     52|     // publicly-readable table -- the exact regression this suite exis…
     53|     expect(migrationSql()).toContain(
       |                            ^
     54|       'ALTER DEFAULT PRIVILEGES IN SCHEMA public REVOKE ALL ON TABLES …
     55|     );

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/3]⎯


 Test Files  1 failed (1)
      Tests  3 failed | 1 passed (4)
   Start at  20:43:59
   Duration  333ms (transform 51ms, setup 0ms, import 67ms, tests 37ms, environment 0ms)

